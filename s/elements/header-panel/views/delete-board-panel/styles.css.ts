import {css} from "lit"

export const styles = css`

h2 {
	color: var(--red);
	margin: 0;
}

p {
	font-size: var(--body-l);
	color: var(--medium-grey);
	font-weight: 500;
	margin: 1em 0;
}

button {
	flex: 1;
}

.btn-row {
	display: flex;
	gap: 0.5em;
}

`
