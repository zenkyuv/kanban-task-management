import {css} from "lit"

export const styles = css`

.panel-background {
	z-index: 1;
}

h2 {
	color: var(--red);
}

p {
	font-size: var(--body-l);
	color: var(--medium-grey);
	font-weight: 500;
}

.btn-row {
	display: flex;
	gap: 0.5em;
}

button {
	flex: 1;
}

`
